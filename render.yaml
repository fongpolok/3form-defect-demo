services:
  - type: web
    name: defect-detection-backend
    runtime: docker
    plan: free
    dockerfilePath: ./backend/Dockerfile.render
    dockerContext: ./backend
    envVars:
      - key: INSPECT_SERVER__CORS_ORIGINS
        value: '["https://fongpolok.github.io"]'
